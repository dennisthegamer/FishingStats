// Root project holds no build logic. The mod is built twice from the shared sources
// in common/src/main by the two loader subprojects:
//   fabric/   -> fishingstats-fabric-mc<mc>-<version>.jar   (net.fabricmc.fabric-loom)
//   neoforge/ -> fishingstats-neoforge-mc<mc>-<version>.jar (net.neoforged.moddev)
// Running "gradlew build" builds both jars.
